'use babel';

import ResizableRightPanelView from '../lib/resizable-right-panel-view';

describe('ResizableRightPanelView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
